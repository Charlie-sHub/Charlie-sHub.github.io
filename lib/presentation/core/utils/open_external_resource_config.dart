/// Browser target used for external links and same-origin document previews.
const externalResourceWindowTarget = '_blank';

/// Browser window features used to prevent opener access on launched tabs.
const externalResourceWindowFeatures = 'noopener,noreferrer';
