# Archived Root Static Site

This directory preserves the pre-Flutter portfolio site that previously lived at the repository root.

Archived contents:
- `index.html`
- `main.css`
- `i/`

Status:
- reference only
- not part of the launch build or deployment path
- replaced by the Flutter Web site sourced from `lib/`, `assets/`, and `web/`
